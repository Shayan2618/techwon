PORT=3002
NODE_ENV=production

# Admin key for viewing contact submissions at GET /api/contact
ADMIN_KEY=techwon_admin_2026

# Email (optional — uncomment and fill in for live email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your_app_password
# NOTIFY_EMAIL=business@techwon.co

# Frontend URL for CORS (when running frontend separately)
FRONTEND_URL=https://techwon.co
